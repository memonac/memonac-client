import React, { useState } from "react";
import ModalContainer from "../../components/Modal";

function Main() {
  const [isModalOpen, setIsModalOpen] = useState(false);

  return <h1>This is memoRoom Main</h1>;
}

export default Main;
